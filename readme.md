# AI Labor Market Impact Web App

An end-to-end Machine Learning web application deployed to Streamlit Cloud. It dynamically tracks and models salary trends against automation variables extracted from the `sarcasmos/ai-society` Kaggle dataset.

## How to Run Locally

1. **Clone the Repository**
2. **Install Dependencies**:
   ```bash
   pip install -r requirements.txt